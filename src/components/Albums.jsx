import React from 'react';
import ListaAlbums from './Albums/ListaAlbums';

//Definimos e componente Albums que engloba a lista de albums.
const Albums = () => {
    return (
        <div>
            <ListaAlbums />
        </div>
    );
};

export default Albums;