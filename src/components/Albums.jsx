import React from 'react';
import ListaAlbums from './Albums/ListaAlbums';

const Albums = () => {
    return (
        <div>
            <ListaAlbums />
        </div>
    );
};

export default Albums;